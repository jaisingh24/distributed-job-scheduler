spring:
  application:
    name: job-scheduler

  datasource:
    url: jdbc:mysql://localhost:3306/job_scheduler
    username: root
    password: junl1234
    driver-class-name: com.mysql.cj.jdbc.Driver






  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  redis:
    host: localhost
    port: 6379


logging:
  level:
    org.hibernate.SQL: WARN